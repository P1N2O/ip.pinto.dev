{
  "name": "ip.8080.li",
  "version": "1.0.0",
  "description": "A simple HTTP server that returns the visitor's IP address in Plain Text, JSON, JSONP or XML format along with optional geographic information.",
  "author": {
    "name": "Manuel Pinto",
    "email": "manuel@pinto.dev",
    "url": "https://manuel.pinto.dev"
  },
  "module": "index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "bun run --watch --hot index.ts",
    "start": "bun run index.ts",
    "build": "bun build index.ts --compile --bytecode --outfile=dist/ip.8080.li"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
